import Arr from '@fuus/array';
import Num from '@fuus/number';
import Str from '@fuus/string';

import Ran from '@fuus/random';
import Ease from '@fuus/easing';

import { delay, debounce, throttle } from '@fuus/main';

export default {
  delay,
  debounce,
  throttle,
  Arr,
  Num,
  Str,
  Ran,
  Ease,
};

export { delay, debounce, throttle } from '@fuus/main';

export { Arr, Num, Str, Ran, Ease };
